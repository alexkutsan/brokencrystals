# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-09T16:19:34.978Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose --file compose.local.yml up -d --build`
- **Port:** 3000

## First-Run Setup

- **Completed:** yes

## Authentication

- **Has auth:** yes
- **Auth object ID:** `a7TLb9HRsVuQQHutmbDRE3`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] App rebuilt for localhost access with permissive session/runtime settings, and rapid POST login verification confirmed no active rate limiting or scanner-blocking auth throttles remain.
- _hint:_ [scan-prep] Inspected the NestJS/Fastify codebase and runtime containers for built-in and code-level rate limiting, throttlers, account lockout, CAPTCHA, IP filtering, and similar controls; found no active framework throttler modules, rate-limit middleware, or DB-backed limiter settings in the running app.
- _hint:_ [scan-prep] Verified and preserved the source-level CSRF relaxation already present in src/auth/csrf.guard.ts so automated POST workflows are not blocked by CSRF validation.
- _hint:_ [scan-prep] Confirmed long-lived permissive session behavior in src/main.ts (rolling session enabled, 10-year cookieExpire, insecure/non-HttpOnly cookie settings already permissive for scanning).
- _hint:_ [scan-prep] Patched compose.local.yml to force the app URL to http://localhost:3000 and run the nodejs service in development mode with a very long MCP_SESSION_TTL_MS value, preventing localhost/probe connectivity and short session issues during scanning.
- _hint:_ [scan-prep] Patched .dockerignore to exclude the runtime .data directory from Docker build context so targeted rebuilds succeed reliably without copying database/runtime artifacts.
- _hint:_ [scan-prep] Rebuilt and restarted only the nodejs service with docker compose -f compose.local.yml up -d --build nodejs, then confirmed the container is healthy and serving on localhost:3000.
- _hint:_ [scan-prep] Verified the real auth processing path by issuing 6 rapid POST requests to POST /api/auth/login with the seeded admin account; all returned HTTP 201 with normal auth responses and no 429/throttle/lockout errors.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/admin/info. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"message":"Forbidden resource","error":"Forbidden","statusCode":403}), login=201, auth=200 ({})

## Hints

**startup**
- Start the app with `docker compose --file compose.local.yml up -d --build`. The web app is published on host port 3000 and serves `http://127.0.0.1:3000/` with HTTP 200. Compose also starts db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-09T16:19:34.978Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose --file compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {}
  },
  "setup": {
    "completed": true
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "a7TLb9HRsVuQQHutmbDRE3",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "{\"email\":\"test@test.com\",\"firstName\":\"Test\",\"lastName\":\"User\",\"password\":\"Passw0rd123\",\"op\":\"basic\"}",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] App rebuilt for localhost access with permissive session/runtime settings, and rapid POST login verification confirmed no active rate limiting or scanner-blocking auth throttles remain.",
      "[scan-prep] Inspected the NestJS/Fastify codebase and runtime containers for built-in and code-level rate limiting, throttlers, account lockout, CAPTCHA, IP filtering, and similar controls; found no active framework throttler modules, rate-limit middleware, or DB-backed limiter settings in the running app.",
      "[scan-prep] Verified and preserved the source-level CSRF relaxation already present in src/auth/csrf.guard.ts so automated POST workflows are not blocked by CSRF validation.",
      "[scan-prep] Confirmed long-lived permissive session behavior in src/main.ts (rolling session enabled, 10-year cookieExpire, insecure/non-HttpOnly cookie settings already permissive for scanning).",
      "[scan-prep] Patched compose.local.yml to force the app URL to http://localhost:3000 and run the nodejs service in development mode with a very long MCP_SESSION_TTL_MS value, preventing localhost/probe connectivity and short session issues during scanning.",
      "[scan-prep] Patched .dockerignore to exclude the runtime .data directory from Docker build context so targeted rebuilds succeed reliably without copying database/runtime artifacts.",
      "[scan-prep] Rebuilt and restarted only the nodejs service with docker compose -f compose.local.yml up -d --build nodejs, then confirmed the container is healthy and serving on localhost:3000.",
      "[scan-prep] Verified the real auth processing path by issuing 6 rapid POST requests to POST /api/auth/login with the seeded admin account; all returned HTTP 201 with normal auth responses and no 429/throttle/lockout errors.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"\".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/admin/info. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}), login=201, auth=200 ({})"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "a7TLb9HRsVuQQHutmbDRE3",
      "updatedAt": "2026-07-09T13:16:37.848Z",
      "createdAt": "2026-07-09T13:16:37.848Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  },
                  {
                    "name": "Accept",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "{{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(.+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "3decAgzP1J7jWiEvEp7Q8y",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/one/admin/info",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "iHQPfxUR2LyzemZtJ79jLg"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Start the app with `docker compose --file compose.local.yml up -d --build`. The web app is published on host port 3000 and serves `http://127.0.0.1:3000/` with HTTP 200. Compose also starts db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
